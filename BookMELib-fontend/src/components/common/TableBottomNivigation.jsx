import Icon from "@mdi/react";
import { mdiChevronLeft, mdiChevronRight } from "@mdi/js";

// This is for

const TableBottomNavigation = ({ children }) => {
  return <>{children}</>;
};

export default TableBottomNavigation;
